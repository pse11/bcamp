<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<!-- 위의 코드를 넣어줘야 configuration태그 작성 가능 -->
<configuration>
	<properties resource="db/db.properties"/>
	<typeAliases>
		<typeAlias type="com.board.dto.BoardDto" alias="BoardDto"/>
	</typeAliases>
	
	<environments default="development">
		<environment id="development">	<!-- default 와 id가 같아야한다. -->
			<transactionManager type="JDBC"/>
			<dataSource type="POOLED">
				<property name="driver" value="${driver}"/>
				<property name="url" value="${url}"></property>
				<property name="username" value="${username}"></property>
				<property name="password" value="${password}"></property>
			</dataSource>
		</environment>
	</environments>
	
	<mappers>
		<mapper resource="db/board_mapper.xml"></mapper>
	</mappers>
</configuration>