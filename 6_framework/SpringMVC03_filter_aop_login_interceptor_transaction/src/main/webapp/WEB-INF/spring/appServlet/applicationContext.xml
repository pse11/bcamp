<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd">
	
	<!-- Root Context: defines shared resources visible to all other web components -->
		
	<!-- dataSource : db 연결 담당-->
	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="oracle.jdbc.driver.OracleDriver"/>
		<property name="url" value="jdbc:oracle:thin:@localhost:1521:orcl"/>
		<property name="username" value="MULTI"/>
		<property name="password" value="MULTI"/>
	</bean>
	
	<!-- SqlSessionfactoryBean : mybatis 환경설정. dataSource 연결 물려줌. mybatis설정 config.xml사용 -->
	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="configLocation" value="WEB-INF/spring/sqls/config.xml"/>		
	</bean>
	
	<!-- sqlSessionTemplate : mybatis의 sqlsession을 스프링에서 안전하게 관리하기 위한 래퍼, dao에서 주로 @Autowired해서 이거 주입받아 쿼리 실행-->
	<bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
    	<constructor-arg ref="sqlSession"/>
	</bean>
	
	<!-- 트랜잭션매니저 : 스프링에서 트랜잭션 관리할 때 사용, @Transactional쓸때 이 트랜잭션 매니저가 자동으로 동작해서 commit/rollback 컨트롤한다.-->
	<!-- @Transactional이 붙은 서비스 메소드 실행 시 트랜잭션 시작-> Dao(mybatis) insert/update/delete실행 -> 예외없으면 commit-> 예외 나면 rollback -->
	<bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource"></property>
	</bean>
</beans>
